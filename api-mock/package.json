{
    "name": "api-mock",
    "version": "1.0.0",
    "description": "simple mock of api with auth",
    "main": "api.js",
    "scripts": {
        "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
        "start": "npm run build && node dist/api.js"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "http-status-codes": "^1.3.0",
        "json-server": "^0.12.1",
        "jsonwebtoken": "^8.1.1"
    },
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-es2015": "^6.24.1",
        "rimraf": "^2.6.2"
    }
}
